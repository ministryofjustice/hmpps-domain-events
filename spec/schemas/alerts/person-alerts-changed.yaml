contentType: application/json
name: person.alerts.changed
payload:
  properties:
    additionalInformation:
      properties: { }
      type: object
    description:
      example: The alerts for a given person have been updated in the alerts service
      type: string
    detailUrl:
      example: https://alerts-api-dev.hmpps.service.justice.gov.uk/prisoner/A7779DY/alerts - for more details about options see https://alerts-api-dev.hmpps.service.justice.gov.uk/swagger-ui/index.html#/prisoner-alerts-controller/retrievePrisonerAlerts
      type: string
    eventType:
      example: person.alerts.changed
      type: string
    occurredAt:
      example: '2024-07-02T16:26:36.890967112+01:00'
      type: string
    personReference:
      properties:
        identifiers:
          items:
            - properties:
                type:
                  example: NOMS
                  type: string
                value:
                  example: A7779DY
                  type: string
              type: object
          type: array
      type: object
    version:
      example: 1
      type: integer
  type: object
summary: The alerts for a given person have changed. The detail url default will provide all the latest alerts for a prisoner. These can be filtered to only active alerts. Please see see https://alerts-api-dev.hmpps.service.justice.gov.uk/swagger-ui/index.html#/prisoner-alerts-controller/retrievePrisonerAlerts for full api spec.
title: person.alerts.changed
